const StudentDetails = () =>{
    // logic 

    // UI 

    return (
        // container wrapper 
        <p>{student.name}: <span>{student.address}</span></p>
    )
        
    
}

export default StudentDetails